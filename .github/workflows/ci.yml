name: CI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  test-and-quality:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run tests
        run: pytest

      - name: Check test coverage
        run: pytest --cov=budget --cov-fail-under=100

      - name: Check cyclomatic complexity with radon
        run: radon cc budget tests -s -n B

      - name: Check cyclomatic complexity with xenon
        run: xenon --max-absolute B --max-average B --max-modules B budget tests

      - name: Run flake8
        run: flake8 budget tests
